# Keep the build context to what the multi-stage build actually copies.
.git
.github
docs
e2e
compose.yaml
Dockerfile
.dockerignore
.gitignore
*.md

# Operator-owned, secret-bearing: never part of the build context.
.env
config.yaml
